# Token soup probe

UUIDv7 SHA-256(raw bytes) SQL NULL frozen single daemon writer no compression
request body bytes are opaque response body bytes are opaque misleading success
OpenAI-compatible `/v1/chat/completions` Authorization bounded body bounded frame
bounded queue bounded processing no lossy transformation
